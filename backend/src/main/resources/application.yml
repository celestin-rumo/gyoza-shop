spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/gyoza}
    username: ${DB_USERNAME:gyoza}
    password: ${DB_PASSWORD:gyoza}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true