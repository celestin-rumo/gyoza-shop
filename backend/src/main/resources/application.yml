spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gyoza
    username: gyoza
    password: gyoza

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true